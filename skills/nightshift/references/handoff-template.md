# Nightshift handoff — [작업 제목]

필수 7개 절만 채우면 유효하다. 일일 handoff는 40–60줄을 목표로 하고, 복잡한 실험이나 장기 작업에만 필요한 세부 절을 추가한다.

<!-- 안내: 대괄호 안 내용을 실제 값으로 바꾸고, 작성 안내 주석은 전달 전에 삭제한다. 문서는 현재 상태의 스냅샷이므로 측정 시각과 재검증 명령을 함께 적는다. -->

## 1. 문서 정보 — 필수

<!-- 안내: 어느 문서가 최신인지, 무엇을 먼저 읽어야 하는지 다음 작업자가 추측하지 않게 한다. 모든 시각에는 시간대를 붙인다. -->

- 제목: [작업 제목]
- 작성 시각: [YYYY-MM-DD HH:MM KST]
- 대상 저장소 또는 작업 디렉터리: [절대 경로]
- 이 문서가 대체하는 handoff: [없음 또는 경로와 버전]
- 함께 읽을 문서: [없음 또는 경로 목록]
- 작성자 연락 없이 적용할 기본 원칙: [보수적 기본값]

## 2. 읽기 순서 — 필수

<!-- 안내: 아래 순서가 의존성 순서다. 파일마다 읽어야 할 절과 목적을 한 줄로 쓴다. -->

1. 이 문서 전체 — 미션 우선순위, 금지 사항, 마감 확인
2. [경로] — [읽을 절과 확인할 내용]
3. [경로] — [읽을 절과 확인할 내용]
4. [경로 또는 로그] — [읽을 범위, 예: 마지막 100줄]

## 3. 현재 상태 — 필수

<!-- 안내: 문서를 쓰는 순간의 상태와 야간 실행 직전에 다시 확인할 명령을 함께 적는다. 오래된 수치는 지우거나 측정 시각을 명시한다. -->

- 저장소 루트: [절대 경로]
- 브랜치: [브랜치명]
- Git HEAD: [전체 SHA]
- 작업 트리: [clean 또는 변경 파일 요약]
- 실행 환경: [호스트, 런타임, 가상환경, 핵심 버전]
- 실행 중 작업: [PID·스케줄러 ID·sentinel·로그 경로 또는 없음]
- 사용 중 리소스: [GPU/CPU/메모리/디스크/외부 서비스]

재검증 명령:

```sh
git rev-parse --show-toplevel
git rev-parse HEAD
git status --short
[실행 중 작업과 리소스를 확인하는 읽기 전용 명령]
```

현재 수치:

| 지표 | 값 | 단위 | 전체 조건 | 표본 수 | 측정 시각 | 근거 경로 |
| --- | ---: | --- | --- | ---: | --- | --- |
| [지표명] | [값] | [단위] | [데이터·코드 SHA·설정·하드웨어·seed·프로토콜] | [n] | [시각] | [로그/산출물] |

오래되어 다시 측정해야 할 값:

- [값과 마지막 측정 시각] → [재측정 명령]

## 4. 완료된 것과 증거 — 필수

<!-- 안내: 완료 선언만 쓰지 말고 외부에서 재현 가능한 검사와 원본 경로를 붙인다. 무시해도 되는 경고는 이유와 범위를 제한한다. -->

| 완료 항목 | 검증 명령 | 관측 결과 | 근거 | 관련 커밋 |
| --- | --- | --- | --- | --- |
| [항목] | `[명령]` | [종료 코드·핵심 수치] | [로그/파일 경로] | [SHA 또는 없음] |

무시해도 되는 경고:

- [정확한 경고 문자열 또는 패턴] — [무시 가능한 이유와 적용 범위]

아직 완료로 보면 안 되는 항목:

- [항목] — [빠진 검증 또는 남은 위험]

## 5. 미션 — 필수, 우선순위순

<!-- 안내: 아래 블록을 미션마다 복제한다. 우선순위는 실행 순서다. 결정적 판정이 없으면 PASS를 선언할 수 없으며 최대 PARTIAL이다. -->

### M1. [미션 이름]

- 우선순위와 기대 가치: [P0/P1/P2, 이 순서인 이유]
- 목표: [밤이 끝났을 때 관측 가능해야 하는 상태]
- 범위: [다룰 코드·데이터·서비스]
- 전제조건: [행동 직전에 확인할 라이브 상태]
- 전제조건 확인 명령: `[읽기 전용 명령]`

실행:

```sh
[정확한 launch 명령]
```

장기 작업인 경우:

- 지속 실행 방식: [검증된 nohup / 스케줄러 / 재접속 가능한 long-lived session]
- detached launch 명령: `[nohup 또는 스케줄러 명령]`
- PID, job ID 또는 session ID 저장 위치: [경로]
- invocation 경계 생존 canary: `[실제 detached 방식으로 canary를 시작하고 완료 전에 launch invocation 반환]` → `[다음 invocation에서 command identity·start time 또는 scheduler/session ID 확인]` → `[후속 invocation에서 sentinel·종료 상태 확인]`
- PID namespace 또는 host 식별자: [재접속 시 같은 실행 공간인지 확인할 값]
- 완료 sentinel: [경로 또는 스케줄러 상태]
- 저비용 poll 명령: `[명령]`
- poll 주기: [최대 10분, 호스트의 더 엄격한 갱신 주기 우선]
- 수집 명령: `[명령]`

결정적 판정:

- 판정 명령: `[원시 값을 출력하고 종료 코드가 명확한 명령]`
- 지표: [지표명]
- 기대값 또는 임계값: [값과 비교 방향]
- 허용오차: [±값, 범위 또는 0]
- 표본 수: [n과 반복 횟수]
- 전체 조건 라벨: [데이터/코호트, 코드 SHA, 설정, 하드웨어/서비스, seed, 표본 수, 프로토콜]
- PASS: [수치와 종료 코드로만 정의]
- FAIL: [수치와 종료 코드로만 정의]
- PARTIAL: [판정 가능한 범위와 빠진 근거]

산출물 계약:

- 원시 로그: [경로]
- 결과 파일: [경로와 형식]
- 코드 변경: [허용 경로와 로컬 커밋 여부]
- 보고서에 넣을 핵심 근거: [지표와 경로]

중단 규칙:

- 정상 종료: [조건]
- 인프라 실패 3회: [중단 후 다음 미션으로 이동]
- 동일 실패 5회: [해당 시나리오 폐기]
- 시간 또는 비용 상한: [조건]
- 즉시 fail-closed: [불가역·파괴적·권한 승인·write fence 밖 액션]

복구 가설 사다리:

1. [가장 가능성이 높고 저렴한 가설] → [검사] → [가역적 조치]
2. [다음 가설] → [검사] → [가역적 조치]
3. [마지막 허용 가설] → [검사] → [가역적 조치]

### M2. [미션 이름]

<!-- 안내: M1 블록 전체를 복제해 작성한다. 서로 다른 조건이나 프로토콜의 결과를 같은 지표로 합치지 않는다. -->

- 우선순위와 기대 가치: [값]
- 목표: [값]
- 결정적 판정 명령: `[명령]`
- 기대값·허용오차·표본 수·전체 조건: [값]
- 산출물 경로: [경로]
- 중단 규칙: [값]

## 6. 금지 사항과 자원 한도 — 필수

<!-- 안내: 허용 범위를 경로와 수치로 적는다. 빈칸을 '상식적으로 처리'하게 두지 않는다. -->

Write fence:

- 읽기 허용 루트: [절대 경로 목록]
- 쓰기 허용 루트: [절대 경로 목록]
- 수정 금지 경로: [절대 경로 또는 패턴]
- Git 정책: 사용자 트리에서 checkout/stash/reset/clean 금지, push 금지, [로컬 커밋 허용 여부]
- 병렬 코드 작업: [별도 git worktree 사용 여부와 경로 규칙]

자원 한도:

- GPU: [장치, 개수, 최대 사용률 또는 사용 금지]
- CPU/메모리: [상한]
- 디스크: [중앙 run dir와 대상 저장소 각각의 상한]
- 동시 작업 수: [상한]
- API 사용량·비용: [상한 또는 기본 무제한, 실제 quota/rate limit 준수]
- 네트워크: [허용 범위]
- 블랙아웃: [시간대와 금지 작업]

사용자 개입 없이 금지:

- 원격 시스템 재기동
- 권한 상승 또는 새 권한 승인
- 되돌릴 수 없는 삭제·마이그레이션·배포
- write fence 밖 쓰기
- 비밀값을 journal·로그·보고서에 기록

## 7. 마감과 보고 — 필수

<!-- 안내: 시간대, 최종 보고서 위치, wrap-up 여유를 명시한다. 보고서는 미션 실패와 관계없이 생성한다. -->

- 마감: [YYYY-MM-DD HH:MM KST, 기본 다음 06:30]
- wrap-up 시작: [마감 60분 전 또는 override]
- 보고 형식: [HTML 기본, Markdown 또는 PPT]
- 최종 보고서 디렉터리: [세션 시작 CWD 기본]
- 최종 파일명: [기본 nightshift-report-<run-id>.<format>]
- 중앙 run root: [기본 ~/nightshift]
- heartbeat cadence: [기본 5분, 30초–10분]
- 미션별 poll cadence: [미션 블록 값 우선, 없으면 heartbeat cadence]
- 보고 대상과 발표 시간: [조간보고/스크럼, 기본 3분]
- 보고서에 반드시 포함할 결정 요청: [없음 또는 목록]

## 8. 자료 지도 — 선택

| 자료 | 역할 | 읽을 범위 | 신뢰 수준 | 갱신 시각 |
| --- | --- | --- | --- | --- |
| [경로/URL] | [코드·명세·로그·과거 세션] | [절/라인/마지막 N줄] | [권위/참고/추정] | [시각] |

## 9. 계약과 불변식 — 선택

- [절대로 깨지면 안 되는 입력·출력 계약]
- [데이터 분리, 평가 누수 방지, 호환성 조건]
- [성공 여부와 무관하게 보존할 파일]

## 10. 조건 구분표 — 선택

<!-- 안내: 직접 비교하면 안 되는 실험 조건을 분리한다. 보고서에서도 각 행과 수치에 같은 라벨을 유지한다. -->

| 조건 ID | 데이터/코호트 | 코드 SHA | 설정 | 하드웨어/서비스 | seed | n | 프로토콜 | 비교 가능 대상 |
| --- | --- | --- | --- | --- | --- | ---: | --- | --- |
| C1 | [값] | [값] | [값] | [값] | [값] | [값] | [값] | [조건 ID] |

## 11. 시간 계획 — 선택

| 구간 | 작업 | 진입 조건 | 종료 조건 | 최대 시간 |
| --- | --- | --- | --- | --- |
| [시각–시각] | [미션/대기/wrap-up] | [조건] | [조건] | [분] |

## 12. 정확한 명령 시퀀스 — 선택

```sh
# pre-flight
[명령]

# launch
[명령]

# poll
[명령]

# collect
[명령]

# judge
[명령]
```

## 13. PASS/FAIL 분기 — 선택

- PASS → [다음 미션 또는 추가 검증]
- FAIL, 원인 A → [검사와 가역적 복구]
- FAIL, 원인 B → [검사와 가역적 복구]
- PARTIAL → [남길 산출물과 아침 결정 요청]
- BLOCKED → [사용자가 해야 할 정확한 액션]

## 14. 위험과 대응 — 선택

| 위험 | 조기 신호 | 자동 대응 | 중단 조건 | 아침 보고 내용 |
| --- | --- | --- | --- | --- |
| [위험] | [로그/수치] | [가역적 조치] | [조건] | [결정 또는 후속 작업] |

## 15. 산출물과 보존 정책 override — 선택

<!-- 안내: override가 없으면 종료 30일 후 run 전체 삭제, 종료 7일 후 logs/artifacts 삭제, 최근 3개 run 보존이 적용된다. 미완료 run에는 7일 정리를 적용하지 않는다. -->

- 반드시 보존할 산출물: [경로와 기간]
- 추가 복사 위치: [경로]
- 로그 보존 기간: [기본 또는 일수]
- artifacts 보존 기간: [기본 또는 일수]
- run 전체 보존 기간: [기본 또는 일수]
- 최근 run 무조건 보존 개수: [기본 3]
- 삭제 금지 run ID: [없음 또는 목록]

## 전달 전 검증

- [ ] 필수 7개 절을 모두 채웠다.
- [ ] 모든 경로와 명령을 실제 환경에서 확인했다.
- [ ] 각 미션에 판정 명령, 기대값, 허용오차, 표본 수, 전체 조건 라벨이 있다.
- [ ] launch, poll, collect, judge 명령을 구분했다.
- [ ] detached 작업은 별도 invocation에서도 살아남는지 canary로 확인했고, 실패 시 스케줄러나 재접속 가능한 session을 지정했다.
- [ ] canary launch 호출이 완료 전에 반환됐고, 다음 호출에서 PID뿐 아니라 command identity·start time 또는 scheduler/session ID를 확인했다.
- [ ] write fence, 자원 상한, 블랙아웃, 중단 규칙을 수치와 경로로 적었다.
- [ ] 오래된 수치를 새로 측정하거나 측정 시각을 표시했다.
- [ ] 마감·wrap-up epoch를 명령으로 round-trip 검증했고 두 값의 차이가 예약한 margin과 일치한다.
- [ ] 마감, 시간대, wrap-up 시각, heartbeat/poll cadence, 보고 형식, 출력 위치를 적었다.
- [ ] 비밀값을 넣지 않았고 환경변수 이름이나 credential 파일 경로만 남겼다.
- [ ] 주석과 사용하지 않는 예시 블록을 삭제했다.
